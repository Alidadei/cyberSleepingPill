# AGENTS.md

本仓库对 AI agent 的硬性约束只有一条：**维护 `docs/AGENT_HANDOFF.md`**。

1. **开工前**：先完整阅读 `docs/AGENT_HANDOFF.md`（工具、现状、铁律、任务、经验都在里面）。
2. **收工前**：凡本次改动影响到的部分，必须同步更新该文档——
   - 开发现状（新功能/已下线功能/测试断言数）；
   - 设计决策与取舍档案（站主新拍板、新取舍必须记录在案）；
   - 未来任务（完成的划掉，新增的补上）；
   - 经验教训（本次踩的新坑必须沉淀一条）。
3. 文档顶部「最后更新」日期随之刷新。
4. 文档更新与代码改动放在**同一个 commit**，不允许账实分离。
